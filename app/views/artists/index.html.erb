<h1 class="page-header">Artists Matching <%= params[:artist_name] %></h1>
<ul class="list-group list-group-flush">
    <% @artists.each do |artist| %>
        <li class="list-group-item"><%= artist.name %> | <%= link_to "Show", artist_path(artist.id) %> </li>
        </ul>
    <% end %>
